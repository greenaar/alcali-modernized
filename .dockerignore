.*
# ...but the frontend stage needs the eslint ignore list, otherwise
# vue-cli-service lints the vendored local-echo sources and the build fails.
!.eslintignore
*.md
!README*.md
**/node_modules
.venv
*.sqlite3
smoke-server.*
.coverage
docs/*
docker/saltconfig
docker/sql
docker-compose.yml
**/tests
